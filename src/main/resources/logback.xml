<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ✅ Định nghĩa property cho log directory -->
    <property name="LOG_DIR" value="logs" />

    <!-- ========================================
         Console Appender (for Development)
         ======================================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ========================================
         File Appender (GHI ĐÈ mỗi lần khởi động)
         ======================================== -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <!-- ✅ Sử dụng property -->
        <file>${LOG_DIR}/viettech.log</file>

        <!-- ✅ QUAN TRỌNG: append=false để ghi đè -->
        <append>false</append>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ========================================
         Error File Appender (GHI ĐÈ mỗi lần khởi động)
         ======================================== -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.FileAppender">
        <!-- ✅ Sử dụng property -->
        <file>${LOG_DIR}/error.log</file>

        <!-- ✅ QUAN TRỌNG: append=false để ghi đè -->
        <append>false</append>

        <!-- Only log ERROR level -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n%ex{full}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ========================================
         Root Logger Configuration
         ======================================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="ERROR_FILE" />
    </root>

    <!-- ========================================
         Package-specific Log Levels
         ======================================== -->

    <!-- Your application code: DEBUG level -->
    <logger name="viettech" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="ERROR_FILE" />
    </logger>

    <!-- Hibernate: Only WARN and above -->
    <logger name="org.hibernate" level="WARN" />
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />

    <!-- HikariCP: INFO level -->
    <logger name="com.zaxxer.hikari" level="INFO" />

    <!-- Spring (if used): INFO level -->
    <logger name="org.springframework" level="INFO" />

    <!-- Apache libraries: WARN level -->
    <logger name="org.apache" level="WARN" />

</configuration>