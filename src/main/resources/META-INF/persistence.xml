<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

    <persistence-unit name="VietTech" transaction-type="RESOURCE_LOCAL">

        <!-- ✅ Provider: Dùng Hibernate thay vì EclipseLink -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- ✅ Auto scan entities trong package viettech.entity -->
        <exclude-unlisted-classes>false</exclude-unlisted-classes>

        <!-- ✅ KHÔNG CẦN properties ở đây! -->
        <!-- Tất cả config sẽ override trong JPAConfig.java -->
        <properties>
            <!-- Để trống hoặc chỉ có comment -->
            <!-- All configurations will be set programmatically in JPAConfig.java -->
        </properties>

    </persistence-unit>
</persistence>